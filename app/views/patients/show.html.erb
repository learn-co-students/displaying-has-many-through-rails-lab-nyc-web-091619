<h1><%= @patient.name.titlecase %></h1>

<h3>Appointments:</h3>

<% @appointments.each do |appointment| %>
  <p><%= link_to appointment.to_readable, appointment_path(appointment) %>, with <%= link_to appointment.doctor.name, doctor_path(appointment.doctor) %></p>

<% end %>