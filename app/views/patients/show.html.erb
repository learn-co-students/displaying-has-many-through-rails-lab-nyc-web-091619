<h1><%= @patient.name.titlecase %></h1>

<h3>Appointments:</h3>

<% @appointments.each do |appt| %>
  <p><%= link_to appt.to_readable, appointment_path(appt) %>, with <%= link_to appt.doctor.name, doctor_path(appt.doctor) %></p>

<% end %>
